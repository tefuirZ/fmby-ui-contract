# Reference Snapshot — DO NOT EDIT

本目录是 fmby 主仓 `apps/web/`（`@fmby/web`）的 **只读源码快照**。

- ❌ 不要在本仓库内修改这里的代码——改动不会回流到 fmby 主仓。
- ❌ 不要把它当作 contract 的一部分；contract 的权威定义在仓库根的 `api/` `features/` `design/` `schemas/`。
- ✅ 用作行为/视觉/调用方式的参考。
- ✅ 想跟主干请到 fmby 主仓拉新版。

剔除项：`node_modules/`、构建产物、`.env*`、`*.tsbuildinfo`、`.map`、`.bak`。

如需可运行：复制到独立工程目录，`pnpm install` 后按主仓约定连接后端。
